<requestHandler name="/replication" class="solr.ReplicationHandler">
  <lst name="slave">
    <str name="masterUrl">${solr.master.url}/solr/${solr.core.name}/replication</str>
    <str name="pollInterval">00:10:00</str>
  </lst>
</requestHandler>
